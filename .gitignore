/_build/
/deps/
/doc/
/dist/
/.elixir_ls/

/rust/ourocode_ipc/target/

# Build artifact (escript) — produced by `mix escript.build`
/ourocode

# Local runtime state (interview/journal working dirs)
/.ourocode/

# Stray seed artifacts
/seed_*.yaml

.om*/
*.dump
docs/rubrics/

# issue-* workspace — evidence only stays committed so issue comments render
.issue/**
!.issue/*/
!.issue/*/evidence/
!.issue/*/evidence/**
.issue/**/.auth.json
.issue/**/storage-state.json
